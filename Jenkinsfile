pipeline {
    agent any

    // triggers {
    //     githubPullRequest(
    //         useGitHubHooks: true // Enable GitHub webhook integration
    //     )
    // }

    stages {
        stage('Build') {
            agent any
            steps {
                echo ' -------------------------Build Stage --------------------------- '
            }
        }
    }
}